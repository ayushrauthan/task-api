# Stage 6 — Publishing evidence

The scraper is public in the `task-api` repository under `scraper/`. The README contains the setup command, schema, politeness rules, ethics note, limitation, and a real clean run report.

The repository also runs five parser tests plus clean-run and broken-page CI checks. Generated cache and runtime JSON are excluded from commits.
