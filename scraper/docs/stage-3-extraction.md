# Stage 3 — Raw extraction

Each book detail page produces the eight required raw fields: title, product URL, price text, availability text, rating text, description, source catalogue page, and fetched timestamp.

Selectors are scoped to the product area. Missing descriptions remain `null`; scraped text is never invented.
