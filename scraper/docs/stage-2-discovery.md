# Stage 2 — Discovery

The scraper follows the catalogue's own `next` link rather than hardcoding book URLs. It stops after page 3 and converts relative book links to absolute URLs with the URL API.

Verified checkpoint: 3 catalogue pages, 60 discovered links, 60 unique product URLs.
