# Stage 0 — Target classification

Target: Books to Scrape sandbox.

Scope: first three catalogue pages only, producing 60 book URLs.

Robots check: `https://books.toscrape.com/robots.txt` returned HTTP 404, recorded as “no robots file found”. The missing file is not treated as permission for another target.

The scraper is intentionally scoped to the practice sandbox and is not to be reused on another site without checking that site's rules and terms first.
