# Parser fixtures

This folder is reserved for small saved HTML fixtures used to test extraction without contacting the live site. The core scraper itself does not need fixtures to run.

Recommended fixture cases from A9: missing description, extra whitespace, duplicate URL, and malformed product markup.
